{
  "name": "crypto",
  "version": "1.0.0",
  "private": true,
  "description": "SAPUI5 crypto dashboard with Binance charts",
  "scripts": {
    "start": "ui5 serve --open index.html",
    "start:port": "ui5 serve --open index.html --port 8080"
  },
  "devDependencies": {
    "@ui5/cli": "^4.0.8"
  }
}
